// Внешняя обработка «SmartSale: приём заявок клиентов».
// АРМ менеджера: по заявкам агентов (справочник SmartSale_ЗаявкиКлиентов)
// заводит типовые Партнёра и Контрагента. Логика — в модуле формы. Здесь —
// сведения для регистрации дополнительной обработки в базе.

#Область ПрограммныйИнтерфейс

Функция СведенияОВнешнейОбработке() Экспорт

	ПараметрыРегистрации = Новый Структура;
	ПараметрыРегистрации.Вставить("Вид", "ДополнительнаяОбработка");
	ПараметрыРегистрации.Вставить("Версия", "1.0");
	ПараметрыРегистрации.Вставить("Наименование", "SmartSale: приём заявок клиентов");
	ПараметрыРегистрации.Вставить("БезопасныйРежим", Ложь);
	ПараметрыРегистрации.Вставить("Информация",
		"Разбор заявок агентов на новых клиентов: создание партнёра и контрагента.");

	ТаблицаКоманд = Новый ТаблицаЗначений;
	ТаблицаКоманд.Колонки.Добавить("Представление");
	ТаблицаКоманд.Колонки.Добавить("Идентификатор");
	ТаблицаКоманд.Колонки.Добавить("Использование");
	ТаблицаКоманд.Колонки.Добавить("ПоказыватьОповещение");
	ТаблицаКоманд.Колонки.Добавить("Модификатор");

	СтрокаКоманды = ТаблицаКоманд.Добавить();
	СтрокаКоманды.Представление = "SmartSale: приём заявок клиентов";
	СтрокаКоманды.Идентификатор = "ОткрытьФорму";
	СтрокаКоманды.Использование = "ОткрытиеФормы";
	СтрокаКоманды.ПоказыватьОповещение = Ложь;

	ПараметрыРегистрации.Вставить("Команды", ТаблицаКоманд);

	Возврат ПараметрыРегистрации;

КонецФункции

#КонецОбласти
