# Версии закреплены осознанно. В соседнем проекте развёртывание поставило
# «самое свежее», приехала Starlette с изменённой подписью TemplateResponse,
# и все страницы отдали 500 без единой правки в коде.
#
# Обновлять так: поднять версию здесь, проверить локально, потом разворачивать.

fastapi==0.141.1
starlette==1.6.0
uvicorn==0.40.0
jinja2==3.1.6
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
python-multipart==0.0.20
python-dotenv==1.0.1
